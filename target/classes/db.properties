# ─────────────────────────────────────────────────────────
#  Aadhaar Secure Travel Identity — Database Configuration
#  Place this file in: WebContent/WEB-INF/db.properties
#  OR on the classpath root (src/)
# ─────────────────────────────────────────────────────────

# MySQL connection
db.url      = jdbc:mysql://localhost:3306/aadhaar_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
db.username = root
db.password = suraj121

# Connection pool (using manual pool via DBConnection)
db.driver   = com.mysql.cj.jdbc.Driver
db.poolSize = 10
